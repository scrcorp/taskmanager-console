/**
 * 임시 기능 스위치.
 *
 * 되돌리기 쉽도록 UI 노출 여부만 여기서 끈다. 서버 API/권한은 그대로 살아 있으므로
 * 다시 켤 때는 플래그 값만 true 로 바꾸면 된다.
 */

/** 스케줄 히스토리(감사 로그) 항목 삭제 버튼 노출 여부. 임시 비활성. */
export const SCHEDULE_HISTORY_DELETE_ENABLED = false;
