"use client";

/**
 * Bulk Add (여러 직원 한 번에 생성) — 추후 구현 (#4).
 * 라우트/탭 자리는 지금 잡아두고, 실제 grid 입력 UI 는 다음 작업.
 */

import React from "react";

export default function BulkAddPage(): React.ReactElement {
  return (
    <div className="bg-card border border-border rounded-xl p-12 text-center">
      <p className="text-text-secondary font-medium">Bulk add is coming soon.</p>
      <p className="text-text-muted text-sm mt-1">
        You&apos;ll be able to create multiple staff at once here.
      </p>
    </div>
  );
}
